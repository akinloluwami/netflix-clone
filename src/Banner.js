import React, {useState} from 'react'

function Banner() {
    return (
        const [movie, setMovie] = useState([]);

        useEffect(() => {
            
            }
        }, [])

        <header>


            
        </header>
    )
}

export default Banner
